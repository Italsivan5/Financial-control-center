# Supabase setup — גרסה 1.2

האפליקציה כבר מכילה את Project URL ואת ה-Publishable Key שסיפקת.

## פעם אחת בלבד
1. היכנס לפרויקט ב-Supabase.
2. פתח SQL Editor.
3. פתח את `supabase_schema_v12.sql`, העתק את כל התוכן ולחץ Run.
4. ודא שב-Authentication > Providers האפשרות Email פעילה.
5. פתח את `index.html`.
6. לחץ `כניסה לענן` > `יצירת חשבון`.

אם Confirm email פעיל, אשר את המייל ואז חזור ובצע כניסה.

## מה קורה לאחר מכן
- נוצרת אוטומטית משפחה עבור המשתמש הראשון.
- המידע נשמר גם ב-Supabase וגם ב-cache מקומי.
- אם אין אינטרנט, שינויים נשמרים מקומית ונשלחים כשחוזר החיבור.
- Realtime מעדכן מכשירים אחרים שמחוברים לאותו חשבון.
- אם יש כבר נתונים מקומיים ובענן אין נתונים, תוצע העלאה אוטומטית.

## אבטחה
האפליקציה משתמשת רק ב-Publishable Key, שמיועד ל-Frontend.
אין להכניס ל-HTML Secret Key או Service Role Key.
RLS מגביל את הגישה לנתוני המשפחה של המשתמש המחובר.
